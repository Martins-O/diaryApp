spring:
  datasource:
    url: jdbc:h2:mem:diary/datasource
    driverClassName: org.h2.Driver
    username:
    password:

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create

    show-sql: true



server:
  port: 9092

